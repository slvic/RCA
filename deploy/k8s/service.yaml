apiVersion: v1
kind: Service
metadata:
  name: rca-agent
  namespace: monitoring
  labels:
    app: rca-agent
spec:
  selector:
    app: rca-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
